import type { Theme, PaletteMode, ThemeOptions } from '@mui/material';
import type { CustomColorsOptions } from '../types';
export default function createOverridedTheme(mode: PaletteMode, overrides?: ThemeOptions | ThemeOptions[], customColors?: CustomColorsOptions): Theme;
